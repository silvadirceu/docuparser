from typing import Dict, Any


class DoclingEngine:
    def process(self, pdf_path_or_bytes: Any) -> Dict[str, Any]:
        """
        Process PDF with Docling.
        """
        return {"raw_text": "Docling output placeholder", "tables": []}
